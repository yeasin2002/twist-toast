# Changesets

This folder stores release intent files used by Changesets.

- Add a changeset for each consumer-facing change: `pnpm changeset`
- Release PR versions/changelogs: `pnpm changeset:version`
- Publish from CI: `pnpm changeset:publish`

